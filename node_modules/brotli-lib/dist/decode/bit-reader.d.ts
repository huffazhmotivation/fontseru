import { BrotliInput } from './streams';
export declare const BROTLI_IBUF_MASK: number;
export declare class BrotliBitReader {
    static readonly READ_SIZE = 4096;
    static readonly IBUF_MASK: number;
    input_: BrotliInput;
    byte_buffer_: Uint8Array;
    short_buffer_: Int16Array;
    accumulator32_: number;
    bit_offset_: number;
    half_offset_: number;
    tail_bytes_: number;
    end_of_stream_reached_: number;
    constructor(input: BrotliInput);
    reset(): boolean;
    readMoreInput(): void;
    private bytesToNibbles;
    private readInput;
    private checkHealth;
    private prepare;
    private reload16;
    private readFewBits;
    private readManyBits;
    peekBits(n_bits: number): number;
    skipBits(n_bits: number): void;
    alignToByte(): number;
    copyRawBytes(dst: Uint8Array, offset: number, length: number): void;
    private halfAvailable;
    readBits(n_bits: number): number;
}
//# sourceMappingURL=bit-reader.d.ts.map