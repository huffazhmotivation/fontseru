export declare class BitWriter {
    buffer: Uint8Array;
    pos: number;
    private flushedBytePos;
    constructor(initialSize?: number);
    private ensureCapacity;
    writeBits(nBits: number, value: number): void;
    writeBitsLong(nBits: number, value: bigint): void;
    writeBit(bit: number): void;
    writeByte(byte: number): void;
    writeBytes(bytes: Uint8Array): void;
    alignToByte(): number;
    get bytePos(): number;
    get bitOffset(): number;
    reset(): void;
    takeBytes(): Uint8Array;
    finish(): Uint8Array;
    prepareStorage(): void;
}
export declare function writeVarInt(writer: BitWriter, value: number): void;
export declare function encodeWindowBits(lgwin: number, largeWindow: boolean): {
    value: number;
    bits: number;
};
//# sourceMappingURL=bit-writer.d.ts.map