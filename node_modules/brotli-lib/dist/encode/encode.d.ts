import { BitWriter } from './bit-writer';
import { EncoderParams, EncoderMode } from './enc-constants';
import { SimpleHasher } from './hash-simple';
import { HashChainHasher } from './hash-chains';
import { BinaryTreeHasher } from './hash-binary-tree';
import { Command } from './command';
export interface BrotliEncodeOptions {
    quality?: number;
    lgwin?: number;
    mode?: EncoderMode;
    sizeHint?: number;
}
export interface BrotliEncoderState {
    params: EncoderParams;
    hasher: SimpleHasher | HashChainHasher | BinaryTreeHasher | null;
    distCache: Int32Array;
    lastInsertLen: number;
    commands: Command[];
    numLiterals: number;
    inputPos: number;
    lastProcessedPos: number;
    ringBuffer: Uint8Array;
    ringBufferMask: number;
    prevByte: number;
    prevByte2: number;
    isInitialized: boolean;
    isLastBlockEmitted: boolean;
    writer: BitWriter;
}
export declare function brotliEncode(input: Uint8Array, options?: BrotliEncodeOptions): Uint8Array;
export declare class BrotliEncoder {
    private state;
    constructor(options?: BrotliEncodeOptions);
    private initialize;
    update(input: Uint8Array): Uint8Array;
    finish(): Uint8Array;
    private processBlock;
}
//# sourceMappingURL=encode.d.ts.map