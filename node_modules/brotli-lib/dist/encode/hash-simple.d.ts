import { HasherSearchResult } from './match';
export declare const Q2_BUCKET_BITS = 16;
export declare const Q3_BUCKET_BITS = 17;
export declare const HASH_LEN = 5;
export declare const MIN_MATCH_LEN = 4;
export declare const STORE_LOOKAHEAD = 8;
export declare class SimpleHasher {
    private buckets;
    private bucketBits;
    constructor(bucketBits: number, _lgwin: number);
    reset(): void;
    prepare(data: Uint8Array, inputSize: number): void;
    private hashBytes;
    store(data: Uint8Array, mask: number, ix: number): void;
    storeRange(data: Uint8Array, mask: number, ixStart: number, ixEnd: number): void;
    findLongestMatch(data: Uint8Array, ringBufferMask: number, distanceCache: Int32Array, curIx: number, maxLength: number, maxBackward: number, out: HasherSearchResult): void;
}
export declare function createSimpleHasher(quality: number, lgwin: number): SimpleHasher;
//# sourceMappingURL=hash-simple.d.ts.map