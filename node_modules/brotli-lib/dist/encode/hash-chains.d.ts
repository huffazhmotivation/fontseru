import { BackwardMatch, HasherSearchResult } from './match';
export declare const MIN_MATCH_LEN = 4;
export declare const HASH_TYPE_LENGTH = 4;
export declare class HashChainHasher {
    private buckets;
    private chains;
    private bucketBits;
    private windowMask;
    private blockBits;
    private numLastDistancesToCheck;
    constructor(bucketBits: number, blockBits: number, lgwin: number, numLastDistancesToCheck?: number);
    reset(): void;
    private hashBytes;
    store(data: Uint8Array, mask: number, ix: number): void;
    storeRange(data: Uint8Array, mask: number, ixStart: number, ixEnd: number): void;
    findLongestMatch(data: Uint8Array, ringBufferMask: number, distanceCache: Int32Array, curIx: number, maxLength: number, maxBackward: number, out: HasherSearchResult): void;
    findAllMatches(data: Uint8Array, ringBufferMask: number, distanceCache: Int32Array, curIx: number, maxLength: number, maxBackward: number): BackwardMatch[];
}
export declare function createHashChainHasher(quality: number, lgwin: number): HashChainHasher;
//# sourceMappingURL=hash-chains.d.ts.map